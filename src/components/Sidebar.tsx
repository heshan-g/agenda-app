import React from 'react';

const Sidebar = () => {
  return (
    <div>
      <button>+ New</button>
      <button>Import</button>
      <button>Export</button>
    </div>
  );
};

export default Sidebar;
